let arr = [1,2,3,4,5];
// i want to square all the numbers.

let new_arr = arr.map((ele) =>{
    return ele*ele;
});

console.log(new_arr);

// let new_arr = arr.map(ele => ele*ele);
// console.log(new_arr); // This will also work.
