let form = document.getElementById('formid');

form.addEventListener('submit', (e) => {
    // e.preventDefault();
    // alert("The Form has been submitted successfully ✔️");
    console.log("The Form has been submitted successfully ✔️");
});